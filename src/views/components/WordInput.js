// import {useDispatch} from "react-redux";

// const WordInput = (event, value) => {
//   const dispatch = useDispatch();

//   return (
//     <input
//       placeholder="検索ワードの入力..."
//       onChange={(e) => dispatch(event(e.target.value))}
//       value={value}
//     />
//   );
// };

// export default WordInput;
